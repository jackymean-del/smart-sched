import type { DomainWithMetadata, NormalizedDomain } from 'ag-charts-core';
import { AbstractScale } from './abstractScale';
export declare class ColorScale extends AbstractScale<number, string> {
    readonly type = "color";
    readonly defaultTickCount = 0;
    protected invalid: boolean;
    domain: number[];
    range: string[];
    mode: 'continuous' | 'discrete';
    /**
     * User-visible axis domain for the gradient legend. Decoupled from
     * `domain` (which carries interpolation pivots) so that colour-stop
     * positions do not distort the legend axis range.
     */
    displayDomain?: [number, number];
    private parsedRange;
    update(): void;
    normalizeDomains(...domains: DomainWithMetadata<number>[]): NormalizedDomain<number>;
    toDomain(): number | undefined;
    convert(x: number): string;
    invert(): number | undefined;
    getDomainMinMax(): [undefined, undefined] | [number, number];
    protected refresh(): void;
}
