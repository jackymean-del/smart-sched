import { type ColorScaleColorStop, type ColorScaleMode } from 'ag-charts-core';
import type { ColorScale } from './colorScale';
/**
 * Configures a ColorScale from either explicit fills or a fallback continuous
 * range. Extracts the pattern shared by all colour-scale-aware series.
 */
export declare function configureColorScale(colorScale: ColorScale, colorScaleProps: {
    fills: ColorScaleColorStop[];
    domain?: [number, number];
    mode: ColorScaleMode;
}, dataDomain: number[], fallbackRange: string[]): void;
