import { type ReactiveState } from 'ag-charts-core';
import type { EventsHub } from '../../core/eventsHub';
import type { ChartState } from '../chartState';
import type { DataService } from '../data/dataService';
import type { AnimationManager } from '../interaction/animationManager';
import type { ChartLike, UpdateProcessor } from './processor';
export declare class DataWindowProcessor<D extends object> implements UpdateProcessor {
    private readonly chart;
    private readonly eventsHub;
    private readonly chartState;
    private readonly dataService;
    private readonly animationManager;
    private dirtyZoom;
    private dirtyDataSource;
    private readonly lastAxisZooms;
    private readonly cleanup;
    constructor(chart: ChartLike, eventsHub: EventsHub, chartState: ReactiveState<ChartState>, dataService: DataService<D>, animationManager: AnimationManager);
    destroy(): void;
    private onDataLoad;
    private onDataError;
    private onDataSourceChange;
    private onUpdateComplete;
    private onZoomChange;
    private updateWindow;
    private shouldRefresh;
    private getAxisWindow;
    private getDomainPixelExtents;
}
