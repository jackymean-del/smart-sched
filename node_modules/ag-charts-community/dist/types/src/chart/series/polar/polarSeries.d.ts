import type { ChartAnimationPhase, DynamicContext } from 'ag-charts-core';
import { ChartAxisDirection, StateMachine } from 'ag-charts-core';
import type { HighlightNodeDatum } from '../../../core/eventsHub';
import type { ChartRegistry } from '../../../module/moduleContext';
import type { AnimationValue } from '../../../motion/animation';
import { BBox } from '../../../scene/bbox';
import { Group } from '../../../scene/group';
import { type Node } from '../../../scene/node';
import { Selection } from '../../../scene/selection';
import { Path } from '../../../scene/shape/path';
import { Text } from '../../../scene/shape/text';
import { DataModelSeries, type DataModelSeriesConstructorOpts, type DataModelSeriesNodeDataContext, type DataModelSeriesNodeDatum } from '../dataModelSeries';
import { type PickFocusInputs, SeriesNodePickMode } from '../series';
import { type SeriesProperties } from '../seriesProperties';
import type { ShapeFillBBox } from '../shapeUtil';
export type PolarAnimationState = 'empty' | 'ready' | 'waiting' | 'clearing';
export type PolarAnimationEvent = {
    update: PolarAnimationData;
    updateData: undefined;
    highlight: undefined;
    highlightMarkers: undefined;
    resize: PolarAnimationData;
    clear: {
        seriesRect?: BBox;
    };
    reset: undefined;
    skip: undefined;
};
export type PolarAnimationData = {
    duration?: number;
};
type PolarSeriesProperties = {
    angleKey: string;
    angleName?: string;
    angleKeyAxis?: string;
    radiusKey?: string;
    radiusName?: string;
    radiusKeyAxis?: string;
};
export declare const DEFAULT_POLAR_DIRECTION_KEYS: {
    angle: "angleKey"[];
    radius: "radiusKey"[];
};
export declare const DEFAULT_POLAR_DIRECTION_NAMES: {
    angle: "angleName"[];
    radius: "radiusName"[];
};
export type UnknownPolarSeries = PolarSeries<DataModelSeriesNodeDatum, object, SeriesProperties<object> & PolarSeriesProperties, Node<DataModelSeriesNodeDatum>>;
export declare abstract class PolarSeries<TDatum extends DataModelSeriesNodeDatum & {
    legendItemValue?: string;
}, TOpts extends object, TProps extends SeriesProperties<TOpts> & PolarSeriesProperties, TNode extends Node<TDatum>, TLabel = TDatum, TContext extends DataModelSeriesNodeDataContext<TDatum, TLabel> = DataModelSeriesNodeDataContext<TDatum, TLabel>> extends DataModelSeries<TDatum, TOpts, TProps, TLabel, TContext> {
    directions: ChartAxisDirection[];
    protected itemGroup: Group<unknown>;
    getItemNodes(): TNode[];
    protected nodeData: TDatum[];
    getNodeData(): TDatum[] | undefined;
    protected itemSelection: Selection<TDatum, TNode>;
    protected labelSelection: Selection<TDatum, Text<TDatum>>;
    protected highlightSelection: Selection<TDatum, TNode>;
    protected highlightLabelSelection: Selection<TDatum, Text<TDatum>>;
    animationResetFns?: {
        item?: (node: TNode, datum: TDatum) => AnimationValue & Partial<TNode>;
        label?: (node: Text<TDatum>, datum: TDatum) => AnimationValue & Partial<Text<TDatum>>;
    };
    /**
     * The center of the polar series (for example, the center of a pie).
     * If the polar chart has multiple series, all of them will have their
     * center set to the same value as a result of the polar chart layout.
     * The center coordinates are not supposed to be set by the user.
     */
    centerX: number;
    centerY: number;
    /**
     * The maximum radius the series can use.
     * This value is set automatically as a result of the polar chart layout
     * and is not supposed to be set by the user.
     */
    radius: number;
    /**
     * The largest marker size of any series in the same chart. Used to determine the extent of pointer interaction
     * with the series.
     */
    maxChartMarkerSize: number;
    protected animationState: StateMachine<PolarAnimationState, PolarAnimationEvent>;
    constructor({ categoryKey, pickModes, canHaveAxes, animationResetFns, ...opts }: {
        moduleCtx: DynamicContext<ChartRegistry>;
        categoryKey: string | undefined;
        pickModes?: SeriesNodePickMode[];
        canHaveAxes?: boolean;
        animationResetFns?: {
            item?: (node: TNode, datum: TDatum) => AnimationValue & Partial<TNode>;
            label?: (node: Text<TDatum>, datum: TDatum) => AnimationValue & Partial<Text<TDatum>>;
        };
    } & Partial<DataModelSeriesConstructorOpts<TProps>>);
    getKeyAxis(direction: ChartAxisDirection): string | undefined;
    setZIndex(zIndex: number): void;
    resetAnimation(phase: ChartAnimationPhase): void;
    protected abstract nodeFactory(): TNode;
    protected labelFactory(): Text<TDatum>;
    getInnerRadius(): number;
    surroundingRadius?: number;
    computeLabelsBBox(_options: {
        hideWhenNecessary: boolean;
    }, _seriesRect: BBox): BBox | null | Promise<BBox | null>;
    protected getShapeFillBBox(): ShapeFillBBox;
    protected resetAllAnimation(): void;
    protected animateEmptyUpdateReady(_data: PolarAnimationData): void;
    protected animateWaitingUpdateReady(_data: PolarAnimationData): void;
    protected animateReadyHighlight(_data: unknown): void;
    protected animateReadyHighlightMarkers(_data: unknown): void;
    protected animateReadyResize(_data: PolarAnimationData): void;
    protected animateClearingUpdateEmpty(_data: PolarAnimationData): void;
    protected computeFocusBounds(opts: PickFocusInputs): BBox | Path | undefined;
    getSeriesRange(): [number, number];
    isSeriesHighlighted(highlightedDatum: HighlightNodeDatum | undefined, legendItemValues?: string[]): boolean;
}
export {};
