import type { InternalAgColorType, RequireOptional } from 'ag-charts-core';
import { ChangeDetectableProperties } from 'ag-charts-core';
import type { AgMarkerShape, AgSeriesMarkerStyle, AgSeriesMarkerStylerParams, Styler } from 'ag-charts-types';
export declare class SeriesMarker<TParams = never> extends ChangeDetectableProperties {
    enabled: boolean;
    /** One of the predefined marker names, or a marker shape function (for user-defined markers). */
    shape: AgMarkerShape;
    size: number;
    fill?: InternalAgColorType;
    fillOpacity: number;
    stroke?: string;
    strokeWidth: number;
    strokeOpacity: number;
    lineDash: number[];
    lineDashOffset: number;
    itemStyler?: Styler<AgSeriesMarkerStylerParams<unknown, unknown> & RequireOptional<Omit<TParams, 'context'>>, AgSeriesMarkerStyle>;
    private _cachedStyle?;
    onChangeDetection(property: string): void;
    getStyle(): AgSeriesMarkerStyle;
    getDiameter(): number;
}
/** Highlight/selection styles carry an extra `opacity` field via HighlightOptions's StyleMixins. */
export type MergeMarkerStyleSource = AgSeriesMarkerStyle & {
    opacity?: number;
};
type MergeMarkerStyleResult = AgSeriesMarkerStyle & {
    size: number;
    opacity?: number;
};
/** Specialised mergeDefaults: left-most non-undefined wins, no recursion (no source holds plain objects). */
export declare function mergeMarkerStyles(selectionStyle: MergeMarkerStyleSource | undefined, highlightStyle: MergeMarkerStyleSource | undefined, defaultOverride: AgSeriesMarkerStyle & {
    size: number;
}, markerStyle: AgSeriesMarkerStyle, inheritedStyle: MergeMarkerStyleSource | undefined): MergeMarkerStyleResult;
/** Two-source flat merge: applied after itemStyler resolution to overlay the user style on the base. */
export declare function mergeMarkerStylesPair(resolved: MergeMarkerStyleSource | undefined, base: MergeMarkerStyleResult): MergeMarkerStyleResult;
export {};
