import type { Direction, _ModuleSupport } from 'ag-charts-community';
import type { FunnelConnector } from '../funnel/funnelConnector';
type AnimatablePyramidDatum = {
    x: number;
    y: number;
    top: number;
    right: number;
    bottom: number;
    left: number;
};
export declare function applyPyramidDatum(connector: FunnelConnector, { x, y, top, right, bottom, left }: AnimatablePyramidDatum): void;
export declare function preparePyramidAnimationFunctions(direction: Direction): {
    fromFn: _ModuleSupport.FromToMotionPropFn<AnimatablePyramidDatum, FunnelConnector<AnimatablePyramidDatum>, AnimatablePyramidDatum>;
    toFn: _ModuleSupport.FromToMotionPropFn<AnimatablePyramidDatum, FunnelConnector<AnimatablePyramidDatum>, AnimatablePyramidDatum>;
    applyFn: _ModuleSupport.ApplyFn<AnimatablePyramidDatum, FunnelConnector<AnimatablePyramidDatum>, AnimatablePyramidDatum>;
};
export {};
