import { NetworkLayout, type NetworkLayoutUpdateOptions } from './networkLayout';
export interface NetworkTreeLayoutUpdateOptions<TVertex, TEdge> extends NetworkLayoutUpdateOptions<TVertex, TEdge> {
    nodeHeight?: number;
    nodeWidth?: number;
    nodeMaxHeight?: number;
    nodeMaxWidth?: number;
    regularDimensions: boolean;
    hiddenOnCollapse: boolean;
    verticalSpacing: number;
    verticalSpacingExtra: number;
    outerSpacing: number;
    innerSpacing: number;
}
/**
 * A Network Tree Layout presents the nodes in a hierarchical non-circular network, for example an org chart or family
 * tree.
 */
export declare class NetworkTreeLayout<TVertex, TEdge> extends NetworkLayout<TVertex, TEdge> {
    private readonly contentBoundsAccumulator;
    update(options: NetworkTreeLayoutUpdateOptions<TVertex, TEdge>): void;
    protected calculateRegularDimensions(options: NetworkTreeLayoutUpdateOptions<TVertex, TEdge>): void;
    private updateNodes;
    private updateChildren;
    private drawLink;
    private updateOffset;
    private accumulateContentBounds;
}
