import { _ModuleSupport } from 'ag-charts-community';
import { type BoxBounds, type ChartAnimationPhase, type ChartAxisDirection, type DynamicContext, type Point, Vertex } from 'ag-charts-core';
import { NetworkGraph } from './networkGraph';
import type { NetworkLayout, NetworkLayoutUpdateOptions } from './networkLayout';
import { NetworkLinkNode } from './networkLinkNode';
import type { NetworkLinkInterpolation } from './networkTypes';
export type NetworkSeriesDatumIndex = number;
export interface NetworkDatum<NetworkVertex, TNetworkEdge> extends _ModuleSupport.SeriesNodeDatum<NetworkSeriesDatumIndex> {
    vertex: Vertex<NetworkVertex, TNetworkEdge>;
}
export interface NetworkSeriesOptions {
}
export declare class NetworkSeriesProperties extends _ModuleSupport.SeriesProperties<object> {
    readonly tooltip: _ModuleSupport.SeriesTooltip<Omit<any, "context">>;
}
export interface NetworkSeriesContextNodeData<NetworkVertex, TNetworkEdge> extends _ModuleSupport.SeriesNodeDataContext<NetworkSeriesDatumIndex, NetworkDatum<NetworkVertex, TNetworkEdge>> {
    linkData: NetworkLinkDatum<NetworkVertex, TNetworkEdge>[];
    labelData: any;
}
export interface NetworkLinkDatum<NetworkVertex, TNetworkEdge> {
    from: Vertex<NetworkVertex, TNetworkEdge>;
    to: Vertex<NetworkVertex, TNetworkEdge>;
}
/**
 * A Network Series processes data into a graph structure and presents the nodes in a network layout.
 */
export declare abstract class AbstractNetworkSeries<TVertex, TEdge, TGraph extends NetworkGraph<TVertex, TEdge>, TNode extends _ModuleSupport.TranslatableGroup<TDatum>, TDatum extends NetworkDatum<TVertex, TEdge>, TLinkDatum extends NetworkLinkDatum<TVertex, TEdge>, TLayout extends NetworkLayout<TVertex, TEdge>> extends _ModuleSupport.Series<NetworkSeriesDatumIndex, NetworkDatum<TVertex, TEdge>, NetworkSeriesOptions, NetworkSeriesProperties, TDatum, NetworkSeriesContextNodeData<TVertex, TEdge>> {
    properties: NetworkSeriesProperties;
    protected dataModel?: _ModuleSupport.DataModel<any, any, any>;
    protected processedData?: _ModuleSupport.ProcessedData<any>;
    protected readonly graph: TGraph;
    protected readonly layout: TLayout;
    protected readonly viewportGroup: import("packages/ag-charts-community/dist/types/src/scene/transformable").ScalableType<_ModuleSupport.TranslatableGroup<unknown>>;
    protected readonly dataNodeGroup: _ModuleSupport.TranslatableGroup<unknown>;
    protected readonly linkGroup: _ModuleSupport.TranslatableGroup<unknown>;
    protected readonly datumSelection: _ModuleSupport.Selection<TDatum, TNode>;
    protected readonly linkSelection: _ModuleSupport.Selection<NetworkLinkDatum<TVertex, TEdge>, NetworkLinkNode<NetworkLinkDatum<TVertex, TEdge>>>;
    protected contextNodeData?: NetworkSeriesContextNodeData<TVertex, TEdge>;
    protected vertexDatumIndex: Record<string, number>;
    private pendingCollapsedIds?;
    protected seriesRect?: _ModuleSupport.BBox;
    private pendingPanToItemId?;
    constructor(ctx: DynamicContext<_ModuleSupport.ChartRegistry>);
    abstract createNetworkGraph(): TGraph;
    abstract createNetworkLayout(): TLayout;
    abstract nodeFactory(): TNode;
    abstract updateDatumSelection(nodeData: TDatum[], datumSelection: _ModuleSupport.Selection<TDatum, TNode>): void;
    abstract updateDatumNodes(datumSelection: _ModuleSupport.Selection<TDatum, TNode>): void;
    abstract updateLinkNodes(linkSelection: _ModuleSupport.Selection<NetworkLinkDatum<TVertex, TEdge>, NetworkLinkNode<NetworkLinkDatum<TVertex, TEdge>>>): void;
    abstract getRootVertices(): Vertex<TVertex, TEdge>[];
    abstract getLinkInterpolation(from: Vertex<TVertex, TEdge>, to: Vertex<TVertex, TEdge>): NetworkLinkInterpolation;
    abstract getFocusedVertex(): Vertex<TVertex, TEdge> | undefined;
    abstract getDefaultFocusedVertices(): Vertex<TVertex, TEdge>[] | undefined;
    abstract positionDatumNode(node: TNode, groupBBox: _ModuleSupport.BBox, regularBBox?: _ModuleSupport.BBox): _ModuleSupport.BBox | undefined;
    abstract updateOffset(offset: Point): void;
    abstract expandNetworkToItem(itemIdOrIndex: string | number): void;
    abstract expandItem(itemIdOrIndex: string | number, point: Point): void;
    abstract collapseItem(itemIdOrIndex: string | number, point: Point): void;
    dataCount(): number;
    private applyViewportTransform;
    update(_opts: {
        seriesRect?: _ModuleSupport.BBox;
    }): void;
    private maybePanToItem;
    mapFocusBBoxToPanTarget(seriesRect: BoxBounds, focusBBox: Readonly<_ModuleSupport.BBox>): BoxBounds;
    processPendingCollapse(): void;
    protected expand(ids: string[]): void;
    protected makeLayoutUpdateOptions(): NetworkLayoutUpdateOptions<TVertex, TEdge>;
    private linkFactory;
    private updateSelections;
    private updateLinkSelection;
    private updateNodes;
    private getDatumNodeBBox;
    /** Bbox used for layout-sizing; subclasses can override to exclude decorations. */
    protected measureDatumNode(node: TNode): _ModuleSupport.BBox | undefined;
    private layoutDatumNode;
    private layoutLinkNode;
    getCategoryValue(_datumIndex: NetworkSeriesDatumIndex): any;
    datumIndexForCategoryValue(_categoryValue: any): NetworkSeriesDatumIndex | undefined;
    getLegendData(_legendType: _ModuleSupport.ChartLegendType): _ModuleSupport.CategoryLegendDatum[];
    resetAnimation(_chartAnimationPhase: ChartAnimationPhase): void;
    getSeriesDomain(_direction: ChartAxisDirection): {
        domain: never[];
    };
    getSeriesRange(_direction: ChartAxisDirection, _visibleRange: [number, number]): [number, number];
}
