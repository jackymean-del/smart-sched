import { Graph, Vertex } from 'ag-charts-core';
export declare abstract class NetworkGraph<TVertex, TEdge> extends Graph<TVertex, TEdge> {
    abstract vertices(): Generator<Vertex<TVertex, TEdge>, void, undefined>;
}
