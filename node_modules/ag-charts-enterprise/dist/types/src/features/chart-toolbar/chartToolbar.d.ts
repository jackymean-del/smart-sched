import { _ModuleSupport } from 'ag-charts-community';
import { AbstractModuleInstance, type DynamicContext } from 'ag-charts-core';
export declare class ChartToolbar extends AbstractModuleInstance {
    private readonly ctx;
    enabled: boolean;
    private readonly menuMargin;
    private readonly toolbar;
    private readonly menu;
    private menuShowing;
    constructor(ctx: DynamicContext<_ModuleSupport.ChartRegistry>);
    private onLayoutStart;
    private onButtonPressed;
    private updateButton;
    private hidePopover;
    private setChartType;
    private getChartType;
}
