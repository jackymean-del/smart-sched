import type { ClientSideRowModelStage, GridOptions, NamedBean, NestedDataGetter, RefreshModelParams, RowNode, _IRowNodeGroupStage } from 'ag-grid-community';
import { BeanStub } from 'ag-grid-community';
export declare class GroupStage<TData> extends BeanStub implements NamedBean, _IRowNodeGroupStage {
    beanName: "groupStage";
    step: ClientSideRowModelStage;
    readonly refreshProps: (keyof GridOptions<any>)[];
    treeData: boolean;
    grouping: boolean;
    private gosTreeData;
    private pivotMode;
    private hasTreeData;
    private hasRowGrouping;
    private needReset;
    private nested;
    private strategy;
    private strategyType;
    private columnsInvalidated;
    postConstruct(): void;
    invalidateGroupCols(): void;
    destroy(): void;
    getNonLeaf(id: string): RowNode<TData> | undefined;
    getNestedDataGetter(): NestedDataGetter<TData> | null | undefined;
    onPropChange(changedProps: ReadonlySet<keyof GridOptions<any>>): boolean;
    extractData(): TData[];
    execute(params: RefreshModelParams<TData>): boolean | undefined;
    loadLeafs(node: RowNode): RowNode[] | null;
    loadGroupData(node: RowNode<any>): Record<string, any> | null;
    clearNonLeafs(): void;
    private getWantedStrategyType;
    private getStrategy;
}
