import { AgAbstractCellEditor } from 'ag-grid-community';
import type { IFormulaCellEditorParams } from 'ag-grid-community';
import { AgFormulaInputField } from '../../widgets/agFormulaInputField';
export declare class FormulaCellEditor<TData = any, TValue = any, TContext = any> extends AgAbstractCellEditor<IFormulaCellEditorParams<TData, TValue, TContext>, TValue, string> {
    protected eEditor: AgFormulaInputField;
    private focusAfterAttached;
    /** Last raw input passed to `params.parseValue`. Initialised to `this` as an "uncached" sentinel — a DOM raw value can never equal the editor instance, so the first cache check always misses. */
    private cachedRaw;
    /** Memoised parse result for `cachedRaw`. Returned by `getValue()` when the raw input is unchanged across repeated validation/sync passes within an edit session. */
    private cachedParsed;
    constructor();
    initialiseEditor(params: IFormulaCellEditorParams): void;
    private onFormulaInputKeyDown;
    private getStartValue;
    agSetEditValue(value: any): void;
    isPopup(): boolean;
    afterGuiAttached(): void;
    focusIn(): void;
    getValue(): any;
    getValidationElement(): HTMLElement | HTMLInputElement;
    getValidationErrors(): string[] | null;
    private isFormulaText;
}
